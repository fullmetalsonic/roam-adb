# 디버그·재발방지 대장

## 1. 문서 상태

- 작성일: 2026-08-24
- 구현 상태: 3차 기존 Tailscale 경유·ADB 전용 스파이크
- 실제 발견 버그: 17건, 모두 수정 후 자동검증·Windows 실사용 범위 PASS
- 사전 위험은 `기술_아키텍처.md`와 `시험_검증_계획.md`에서 관리한다.

실제 증상이나 시험 실패가 발생하기 전에는 추정 위험을 버그로 등록하지 않는다.

## 2. 심각도

- CRITICAL: 키·데이터 유출, 승인 없는 원격 제어, 광범위한 손상
- HIGH: 핵심 연결 불가, OFF 후 접근 유지, 반복 crash
- MEDIUM: 복구 실패, 특정 네트워크·UI 상태 오류
- LOW: 문구, 정렬, 경미한 사용성 문제

## 3. 상태

- NEW
- REPRODUCED
- ROOT_CAUSE_CONFIRMED
- FIX_IN_PROGRESS
- RETEST
- CLOSED
- N/A

## 4. 버그 목록

| ID | 심각도 | 상태 | 증상 | 직접 원인 | 재발방지 시험 |
|---|---|---|---|---|---|
| BUG-2026-001 | HIGH | CLOSED | Windows TLS server handshake 불가 | 저장소 인증서가 지속 private key와 연결되지 않음 | TLS register/auth 통합시험 |
| BUG-2026-002 | MEDIUM | CLOSED | 한글 경로 JVM test class를 찾지 못함 | Gradle worker 인자 파일의 non-ASCII classpath 해석 | `core-model:test` 실제 실행 |
| BUG-2026-003 | HIGH | CLOSED | 정상 인증 세션도 로그인 제한시간 뒤 종료 가능 | 인증용 CancellationToken을 세션에 재사용 | 로그인 제한시간 뒤 ping 시험 |
| BUG-2026-004 | LOW | CLOSED | Android UI Kotlin 컴파일 실패 | `KeyboardOptions` import 경로 오류 | 전체 Android assemble |
| BUG-2026-005 | MEDIUM | CLOSED | tunnel 성공을 PC 접속으로 오표시하고 OFF 뒤 늦은 결과가 상태를 덮을 가능성 | 상태 의미와 취소 작업 세대 검증 누락 | build/lint PASS, 실기기 회귀는 필요 |
| BUG-2026-006 | LOW | CLOSED | 아이콘 Lint 정리 뒤 AAPT linking 실패 | adaptive icon을 version 없는 폴더로 이동 | 전체 Android assemble |
| BUG-2026-007 | MEDIUM | CLOSED | 인증 휴대폰이 local ADB 연결을 받은 뒤 준비 응답 없이 socket을 계속 점유 가능 | `relay_start` 이후 별도 응답 제한시간이 없었음 | stalled relay 자동 종료 시험 |
| BUG-2026-008 | HIGH | CLOSED(코드) | PC가 15초 안에 접속하지 않으면 Android relay가 끊기고 재게시됨 | 인증용 Android socket read timeout이 relay 대기에도 남음 | Android 전체 build/lint, 실기기 장시간 대기는 필요 |
| BUG-2026-009 | LOW | CLOSED | 연결 모드 UI가 Kotlin compile 실패 | RowScope의 `weight`를 잘못된 top-level 확장으로 import | Android debug/release compile·assemble |
| BUG-2026-010 | LOW | CLOSED | runtime 모듈 Lint가 VPN 사전검사 권한 누락 보고 | 앱 manifest에만 권한을 두어 라이브러리 단독 Lint가 계약을 알 수 없음 | 전체 모듈 `lintDebug` |
| BUG-2026-011 | MEDIUM | CLOSED(코드) | 연결 사전검사 실패 뒤 알림·타일이 ON처럼 남을 수 있음 | Foreground Service가 비실행 오류 상태를 관찰하지 않음 | Android build/lint, 실제 타일 회귀 필요 |

## 5. 개별 기록

### BUG-2026-001 — Windows TLS 인증서 private key 지속 실패

- 발견일: 2026-08-24
- 심각도: HIGH
- 상태: CLOSED

#### 증상·재현

Gateway register/auth 통합시험에서 TLS 연결이 예상치 못한 EOF로 끝났고 서버 진단에 Windows 플랫폼이 ephemeral key를 지원하지 않는다는 암호화 예외가 나타났다.

#### 직접 원인·증거

생성한 self-signed 인증서를 CurrentUser 저장소에 넣는 과정에서 Windows SChannel이 재사용할 수 있는 지속 private key 연결이 보장되지 않았다. 인증서 복사본에는 `HasPrivateKey`가 기대와 다르게 동작했다.

#### 수정·재발방지

인증서를 메모리 PFX로 내보내 `UserKeySet | PersistKeySet`으로 다시 로드한 뒤 저장하고 PFX byte 배열을 즉시 zero 처리했다. TLS 지문 불일치, 등록, 정상·공격자 인증 통합시험을 자동 실행한다.

#### 재시험

- BUILD: PASS, 경고 0·오류 0
- INTEGRATION: PASS

### BUG-2026-002 — 한글 작업경로 Gradle test worker 실패

- 발견일: 2026-08-24
- 심각도: MEDIUM
- 상태: CLOSED

#### 증상·재현

Kotlin 시험 class가 실제 생성됐지만 `ClassNotFoundException: GatewayProfileTest`가 발생했다. Android Gradle의 non-ASCII 검사 해제만으로는 JVM test worker 문제가 해결되지 않았다.

#### 직접 원인·증거

Windows Gradle worker가 UTF-8 인자 파일의 한글 classpath를 정확히 해석하지 못했다. 생성 class 존재와 worker classpath 인자 파일을 대조해 확인했다.

#### 수정·재발방지

소스와 APK 출력은 원래 위치에 두고, pure JVM `core-model` 빌드 출력만 가장 가까운 ASCII 상위 경로의 `.roamadb-build/<path-key>`로 이동했다. Gradle Wrapper 전체 검증에 `test`를 포함했다.

#### 재시험

- `:core-model:test --rerun-tasks`: PASS 3/3
- `test assembleDebug lintDebug`: PASS

### BUG-2026-003 — 인증 제한시간이 정상 세션까지 종료

- 발견일: 2026-08-24
- 심각도: HIGH
- 상태: CLOSED

#### 증상·재현

코드 리뷰에서 TLS·로그인용 15초 token이 인증 후 session read loop에도 그대로 전달되는 것을 확인했다. 장시간 원격 디버깅이 15초 뒤 끊길 수 있었다.

#### 직접 원인·수정

인증 단계와 인증 완료 session의 CancellationToken을 분리했다. 세션은 Gateway 전체 종료 또는 사용자 연결 종료에만 취소된다.

#### 자동 재발방지·재시험

인증 제한시간을 500ms로 둔 뒤 정상 인증하고 750ms 후 같은 세션으로 ping하는 시험을 추가했다. non-loopback listener 거부 시험을 포함한 Gateway 전체 시험 PASS 9/9.

### BUG-2026-004 — Compose KeyboardOptions import 오류

- 발견일: 2026-08-24
- 심각도: LOW
- 상태: CLOSED

Android 첫 전체 빌드에서 `androidx.compose.ui.text.input.KeyboardOptions`를 찾지 못했다. 실제 API 위치인 `androidx.compose.foundation.text.KeyboardOptions`로 수정했고 debug APK assemble과 Lint가 통과했다.

### BUG-2026-005 — READY/PC_CONNECTED 의미와 취소 경쟁

- 발견일: 2026-08-24
- 심각도: MEDIUM
- 상태: CLOSED(코드), 실기기 회귀 필요

#### 증상·원인

Gateway TLS 인증만 성공해도 `PC_CONNECTED`로 표시했으며, 사용자가 OFF한 뒤 blocking 연결이 늦게 반환하면 이전 작업이 ERROR 또는 성공 상태를 덮을 가능성이 있었다.

#### 수정

인증된 tunnel 준비는 `READY`로 표시하고 실제 ADB stream 연결 전에는 `PC_CONNECTED`를 사용하지 않는다. 작업 세대 ID를 도입해 OFF·재시작 이전의 성공/실패 결과를 폐기하고 늦게 열린 session을 닫는다.

#### 재시험

- Android build/test/lint: PASS
- 실제 네트워크 지연 중 OFF: 현장 검증 필요

### BUG-2026-006 — Adaptive icon 폴더와 Lint 권고 충돌

- 발견일: 2026-08-24
- 심각도: LOW
- 상태: CLOSED

`minSdk 35`라 `mipmap-anydpi-v26`가 불필요하다는 Lint 권고에 따라 adaptive icon XML을 version 없는 폴더로 옮겼더니 AAPT가 해당 리소스를 인식하지 않아 debug/release resource linking이 실패했다. Adaptive icon schema가 유효한 `mipmap-anydpi-v26`으로 복구하고 이 파일의 `ObsoleteSdkInt` 권고만 이유를 명시해 억제했다. 전체 Android build/test/lint로 재발을 탐지한다.

### BUG-2026-007 — relay 시작 응답 제한시간 누락

- 발견일: 2026-08-24
- 심각도: MEDIUM
- 상태: CLOSED

#### 증상·직접 원인

독립 코드 리뷰에서 Gateway가 PC loopback ADB client를 받은 뒤 휴대폰의 `relay_ready`를 세션 종료까지 기다리는 것을 확인했다. 인증된 휴대폰이 고장 나거나 악의적으로 응답하지 않으면 PC socket과 relay port를 계속 점유할 수 있었다.

#### 수정·자동 재발방지

`relay_start` 뒤 `relay_ready`에는 인증 제한시간과 같은 길이의 별도 linked timeout을 적용했다. 시험에서는 300ms 제한으로 인증·relay 게시·PC 접속까지 수행한 다음 휴대폰이 준비 응답을 보내지 않도록 하고, 2초 안에 PC socket read가 EOF 0을 반환하는지 확인한다.

#### 재시험

- Gateway build: PASS, 경고 0·오류 0
- Gateway 전체 자동시험: PASS 11/11
- stalled relay local socket 종료: PASS

### BUG-2026-008 — Android 인증 제한시간이 PC 대기 relay에 잔존

- 발견일: 2026-08-24
- 심각도: HIGH
- 상태: CLOSED(코드), 실기기 회귀 필요

#### 증상·직접 원인

독립 코드 리뷰에서 Android `SSLSocket.soTimeout=15초`가 Gateway 인증과 relay 게시 뒤에도 유지되는 것을 확인했다. 사용자가 ON을 누른 뒤 PC에서 15초 안에 `adb connect`를 실행하지 않으면 `relay_start` 대기 read가 시간초과되고 relay가 1.5초 간격으로 재게시될 수 있었다.

#### 수정

TLS handshake, 등록, 인증과 `relay_published` 응답까지만 15초 제한을 사용한다. 인증된 relay가 Gateway loopback에 게시된 직후 Android socket read timeout을 0으로 바꿔 사용자의 PC 접속을 기다리는 동안 끊기지 않게 했다. PC가 접속한 뒤 휴대폰 local adbd 연결에는 별도의 5초 제한을 유지한다.

#### 재시험

- Android `test assembleDebug lintDebug`: PASS
- 실제 Fold8에서 15초 이상 READY 대기 뒤 PC 접속: 현장 검증 필요

### BUG-2026-009 — 연결 모드 RowScope import로 Compose compile 실패

- 발견일: 2026-08-25
- 심각도: LOW
- 상태: CLOSED

#### 증상·재현

3개 연결 방법 UI를 추가한 첫 전체 Android 빌드에서 debug와 release Kotlin compile이 모두 `RowColumnParentData?.weight` 내부 접근 오류로 실패했다.

#### 직접 원인·확인 증거

`Modifier.weight`는 `Row` 수신 범위에서 쓰는 API인데 잘못된 top-level import가 내부 심볼로 해석됐다. 문제 import를 제거한 뒤 같은 `Row` 안의 `Modifier.weight(1f)`가 정상 compile됐다.

#### 수정·재발방지·재시험

잘못된 import만 제거하고 RowScope 호출은 유지했다. 이후 옵션 행 전체에 접근성 `selectable`을 적용했다. `test assembleDebug lintDebug` 전체 재실행 PASS로 debug/release Kotlin compile과 APK assemble을 함께 검증했다.

### BUG-2026-010 — runtime 모듈 단독 Lint의 네트워크 권한 계약 누락

- 발견일: 2026-08-25
- 심각도: LOW
- 상태: CLOSED

#### 증상·재현

컴파일 수정 뒤 전체 Lint에서 `AndroidNetworkRouteChecker`의 `ConnectivityManager.activeNetwork` 호출에 `MissingPermission` 오류 2건이 발생했다.

#### 직접 원인·확인 증거

최종 app manifest에는 `ACCESS_NETWORK_STATE`가 있었지만 해당 API를 소유한 `runtime` Android library manifest에는 선언이 없었다. 병합 앱은 권한을 가졌어도 라이브러리 단독 Lint가 자체 권한 계약을 확인할 수 없었다.

#### 수정·재발방지·재시험

`runtime/src/main/AndroidManifest.xml`에도 `ACCESS_NETWORK_STATE`를 선언했다. 권한은 정상 권한이며 사용자 승인 창을 만들지 않는다. 전체 모듈 `lintDebug`를 다시 실행해 오류 0건, 의존성 새 버전 안내만 남음을 확인했다.

### BUG-2026-011 — 사전검사 오류 뒤 Foreground Service·타일 상태 잔존

- 발견일: 2026-08-25
- 심각도: MEDIUM
- 상태: CLOSED(코드), 실기기 회귀 필요

#### 증상·재현

코드 리뷰에서 Tailscale 미연결 등으로 `ConnectionController.start()`가 `ERROR`로 전환해도 `RoamAdbService`가 계속 foreground에 남고, 타일이 클릭 직후의 ON 표시를 유지할 수 있음을 확인했다.

#### 직접 원인·영향 범위

서비스는 사용자 STOP 명령이나 Android의 서비스 종료 때만 foreground를 내렸다. 연결 상태 Flow의 비실행 오류를 관찰하지 않아 앱 본문은 오류·OFF 버튼 상태인데 알림과 타일은 ON처럼 보이는 불일치가 생길 수 있었다. VPN 사전검사, ADB 포트 누락과 최초 Gateway 접속 실패가 영향을 받는다.

#### 수정·재발방지·재시험

서비스 전용 main coroutine scope에서 연결 상태를 관찰한다. 사용자가 START한 뒤 상태가 비실행으로 바뀌면 foreground 알림을 제거하고 서비스를 종료하며 `TileService.requestListeningState`로 타일을 새로고침한다. 오류로 자체 종료할 때는 `onDestroy`가 `stop()`을 다시 호출해 오류를 OFF로 덮지 않도록 최종 상태를 보존하고, 사용자 STOP 때만 정상 OFF로 전환한다. Android `test assembleDebug lintDebug` PASS. 실제 Fold8 알림·타일 동기화는 현장 검증 필요다.

### BUG-2026-012 — CI 호환 JDK 탐색이 기존 JAVA_HOME 17을 허용

- 발견일: 2026-08-25
- 심각도: LOW
- 상태: CLOSED

#### 증상·재현

GitHub 공개 직전 회귀시험에서 Gateway는 13/13 PASS했지만 Android 빌드가 `languageVersion=21` 도구체인을 찾지 못해 중단됐다. PC에는 JDK 21이 설치돼 있었지만 프로세스의 `JAVA_HOME`은 JDK 17을 가리키고 있었다.

#### 직접 원인·확인 증거

CI에서 설정한 JDK를 재사용하도록 빌드 스크립트를 일반화하면서 `JAVA_HOME/bin/java.exe` 존재만 확인하고 실제 주 버전을 검사하지 않았다. 따라서 유효한 JDK 17 경로를 JDK 21로 잘못 인정했다.

#### 수정·자동 재발방지·재시험

`JAVA_HOME/release`의 `JAVA_VERSION`이 21인지 먼저 검사하고, 그렇지 않으면 설치된 Microsoft JDK 21을 선택하도록 수정했다. 같은 공개 후보에서 `test assembleDebug lintDebug`를 다시 실행해 BUILD SUCCESSFUL과 동일 APK SHA-256을 확인했다. GitHub Actions의 Windows Android job도 Temurin 21을 명시해 이 스크립트를 그대로 실행한다.

### BUG-2026-013 — 공개 Windows 산출물이 승인된 GUI가 아닌 CLI 전용

- 발견일: 2026-08-25
- 심각도: HIGH
- 상태: CLOSED(v0.1.1 코드·실제 GUI 검증)

#### 증상·재현

`v0.1.0-spike` EXE를 더블클릭하면 콘솔이 잠깐 나타난 뒤 종료돼 코드·QR·상태를 볼 수 없었다. 최초 기획과 구현 전 승인안에는 일반 사용자용 버튼 UI, QR/6자리 코드, 진단·연결·해제가 포함돼 있었다.

#### 직접 원인·잘못된 기존 접근

Gateway 프로토콜 스파이크의 CLI를 공개 다운로드 산출물로 그대로 사용하면서 요구사항-릴리스 대조와 더블클릭 사용자 흐름 검증을 생략했다. CLI 내부 기능 통과를 제품 UI 완료로 잘못 확대 해석했다.

#### 수정·자동 재발방지·재시험

WPF `RoamADB Gateway`를 별도 모듈로 추가하고 상태, 시작/정지, 2분 코드, QR, 지문, ADB 작업, 등록 기기 해제를 화면에 배치했다. `v0.1.1_요구사항_재감사.md`에서 릴리스 완료 조건을 명시했다. 실제 창을 실행해 Tailscale 주소 표시, 코드·QR 생성과 중지 시 폐기를 확인했다.

### BUG-2026-014 — WPF 단일 파일 배포본의 네이티브 DLL 누락

- 발견일: 2026-08-25
- 심각도: HIGH
- 상태: CLOSED(설치본·Portable 재시험 PASS)

#### 증상·재현

프레임워크 의존 Release EXE는 열렸지만 Inno Setup으로 설치한 self-contained 단일 EXE는 창을 만들지 못하고 종료됐다. Windows Application 로그에 `.NET Runtime 1026`, `System.DllNotFoundException`, `SetWindowLongPtrWndProc`가 기록됐다.

#### 직접 원인·영향 범위

`PublishSingleFile=true`만 지정하고 WPF가 사용하는 네이티브 라이브러리의 자체 추출 옵션을 넣지 않았다. Portable과 설치본 모두 영향을 받았다.

#### 수정·자동 재발방지·재시험

게시 스크립트에 `IncludeNativeLibrariesForSelfExtract=true`를 추가했다. 이후 완료 조건에 공개 후보 Portable과 설치본의 실제 창 생성 시험을 모두 포함한다.

동일 옵션으로 다시 게시하고 silent 설치한 EXE를 실제 실행했다. GUI 창이 정상 생성·유지됐고 새 `.NET Runtime` 또는 `Application Error` 이벤트가 없었다. 설치본과 Portable은 같은 게시 EXE 바이트를 사용한다.

### BUG-2026-015 — 종료 이벤트 안의 재귀 Close 호출로 crash

- 발견일: 2026-08-25
- 심각도: MEDIUM
- 상태: CLOSED(정상 종료·이벤트 로그 재시험 PASS)

#### 증상·재현

Gateway가 꺼진 상태에서 닫기 단추를 누르면 창은 사라졌지만 Application 로그에 `InvalidOperationException: 창을 닫는 중에는 ... Close ... 호출할 수 없습니다`가 기록됐다.

#### 직접 원인

비동기 `Closing` 이벤트에서 첫 닫기를 취소한 뒤 정리가 즉시 끝나는 경우, 같은 닫기 호출 스택 안에서 `Close()`를 다시 호출했다.

#### 수정·자동 재발방지·재시험

종료 시작 상태를 별도로 두고 첫 이벤트는 항상 취소한다. Gateway 정리 뒤 `DispatcherPriority.ApplicationIdle`에 두 번째 닫기를 예약해 최초 닫기 이벤트가 완전히 반환된 다음 실행한다. 재시험은 정상 종료 코드와 새 Application Error 부재를 함께 확인한다.

설치본 GUI를 열고 일반 닫기 단추로 종료했다. 프로세스가 남지 않았고 종료 시각 이후 Windows Application 로그에 새 오류가 없었다. 이어서 silent uninstall도 exit code 0으로 완료됐다.

### BUG-2026-016 — 두 번째 실행 프로세스가 소유하지 않은 mutex 해제 시도

- 발견일: 2026-08-25
- 심각도: MEDIUM
- 상태: CLOSED(중복 실행 실제 재시험 PASS)

#### 증상·원인

독립 코드 리뷰에서 이미 실행 중인 경우에도 `OnExit`가 무조건 `ReleaseMutex()`를 호출하는 경로를 확인했다. 두 번째 프로세스는 기존 mutex를 소유하지 않으므로 안내창을 닫은 뒤 `ApplicationException`이 발생할 수 있었다.

#### 수정·자동 재발방지·재시험

mutex를 실제로 새로 만든 첫 프로세스만 `_ownsSingleInstance=true`로 표시하고 소유 프로세스만 해제하도록 수정했다. 설치본 첫 창을 유지한 채 같은 EXE를 두 번째로 실행해 “이미 실행 중” 안내를 확인하고 닫았다. 두 번째 프로세스는 종료되고 첫 창만 남았으며, 두 PID 모두 정상 종료한 뒤 새 `.NET Runtime`·`Application Error` 이벤트가 0건임을 확인했다.

### BUG-2026-017 — Android CI의 JDK 21과 core-model 고정 JDK 17 불일치

- 발견일: 2026-08-25
- 심각도: HIGH
- 상태: CLOSED(로컬 JDK 21 전체 재시험 PASS, GitHub 수정 commit 재실행 대상)

#### 증상·직접 원인

GitHub Actions Android job이 `:core-model:compileKotlin` 의존성을 계산하지 못하고 “languageVersion=17 Java installation을 찾을 수 없음”으로 실패했다. CI는 JDK 21을 정상 설치했지만 순수 Kotlin 모듈이 `jvmToolchain(17)`로 빌드 JDK의 정확한 버전까지 17로 고정했다. 반면 공용 빌드 스크립트와 AGP 계약은 JDK 17 또는 21 실행을 허용하고 있어 서로 불일치했다.

#### 수정·재발방지·재시험

`core-model`의 출력 바이트코드 `jvmTarget=17`과 Java source/target 17은 유지하되, 설치 JDK를 17로 강제하는 `jvmToolchain(17)`만 제거했다. 따라서 JDK 17·21 어느 쪽에서도 Java 17 호환 바이트코드를 생성한다. 로컬 `JAVA_HOME`을 JDK 21로 명시한 전체 `test assembleDebug lintDebug`가 PASS했고 APK SHA-256도 기존 후보와 같았다. 수정 commit의 GitHub Actions 결과는 Release 게시 완료 판정에서 별도로 확인한다.

### BUG-2026-018 — Android DER ECDSA 서명을 Gateway가 P1363으로 검증

- 발견일: 2026-08-25
- 심각도: CRITICAL
- 상태: CLOSED(수정 GUI 설치·Fold8 인증·USB 없는 ADB 셸 PASS)
- 영향 버전: Public `v0.1.1-spike` Gateway
- 기기·Android·One UI: SM-F971N, Android 17, One UI 9
- 네트워크: 공식 Tailscale tailnet, PC Gateway `47156`

#### 증상·재현

Fold8의 QR 등록은 성공했지만 `원격 디버깅 켜기`를 누르면 앱 상태가 `연결 오류`, 상세가 `authentication_rejected`로 바뀌었다. PC Gateway는 등록 기기를 보존하고 있었고 Fold8 Tailscale 노드는 온라인이었다.

#### 직접 원인·확인 증거

Android `Signature.getInstance("SHA256withECDSA")`는 ASN.1 DER ECDSA 서명을 생성한다. 프로토콜 문서도 DER를 계약으로 정했지만 Gateway는 서명 형식을 지정하지 않은 .NET `ECDsa.VerifyData` 기본값인 IEEE P1363으로 검증했다. 기존 통합시험은 .NET 클라이언트도 기본 P1363 서명을 만들어 같은 런타임끼리만 통과했기 때문에 Android 상호운용 오류를 탐지하지 못했다.

PC에서 Gateway의 Tailscale 전용 `47156` listener, 등록 기기 JSON, Fold8 Tailscale 온라인 상태와 `tailscale ping` 응답을 확인해 네트워크·등록 실패를 배제했다. Android가 반환한 `authentication_rejected`는 challenge-response 단계의 서명 검증 실패와 일치한다.

#### 수정·자동 재발방지 장치

Gateway 검증을 `DSASignatureFormat.Rfc3279DerSequence`로 고정하고 .NET 시험 클라이언트도 같은 DER 계약으로 서명하도록 수정했다. 서명 검증 단위시험은 DER 형식의 첫 바이트 `0x30`을 명시적으로 확인하므로 예전 P1363 기본값으로 되돌아가면 실패한다.

#### 재시험 결과

- BUILD: PASS, 경고 0·오류 0
- UNIT/INTEGRATION: Gateway 14/14 PASS
- 설치: 수정된 설치본 exit 0, 설치 EXE와 빌드 EXE SHA-256 `B6D50DFB95633E1D9C06D06F8819A0E6861513C619C5EE376758A03A2C8ADBE6` 일치
- 등록 보존: PASS, 기존 Fold8 장치 레코드 유지
- DEVICE E2E: PASS, Fold8 인증 뒤 `127.0.0.1:47157 device`와 USB 없는 셸 확인
- GitHub Release: 아직 갱신하지 않음

### BUG-2026-019 — 무선 ADB 포트 변경과 중계 리스너 잔류

- 발견일: 2026-08-25
- 심각도: HIGH
- 상태: PARTIAL(Gateway 리스너 수정·회귀시험·실기기 PASS, Android 자동 포트 갱신은 후속)
- 영향 버전: Public `v0.1.1-spike`
- 기기·Android·One UI: SM-F971N, Android 17, One UI 9

#### 증상

서명 인증 수정 뒤 일반 중계는 열렸지만 PC `adb connect 127.0.0.1:47157`가 실패하거나 장치가 `offline`이 됐다. 일반 중계를 휴대폰에서 끈 뒤 다시 켜면 `relay_port_unavailable`이 표시됐다.

#### 직접 원인·확인 증거

USB ADB `dumpsys adb`에서 Fold8의 현재 `adb_wifi.tls_port=40713`을 확인했지만 RoamADB에는 과거 포트 `43857`이 저장돼 있었다. Android가 Wi-Fi 상태 변화 뒤 무선 ADB connect 포트를 변경했는데 앱은 수동 저장값을 자동 갱신하지 않았다.

또한 PC Gateway `RunRelayAsync`가 `AcceptTcpClientAsync`로 로컬 ADB 접속을 기다리는 동안 휴대폰 TLS 세션 종료를 감시하지 않는다. 휴대폰이 중계를 끄더라도 로컬 ADB가 아직 접속하지 않았다면 `127.0.0.1:47157` listener가 남아 다음 게시가 `relay_port_unavailable`로 실패했다.

#### 현장 우회·재시험

USB ADB로 현재 포트 `40713`을 읽어 RoamADB 저장값을 갱신했다. 잔류 GUI Gateway를 종료하고 같은 수정 코어의 CLI Gateway를 새 프로세스로 시작해 listener를 정리했다. 이후 RoamADB 일반 중계를 열고 PC에서 `adb connect 127.0.0.1:47157`를 실행했다.

- RoamADB endpoint: `127.0.0.1:47157 device product:h8qksx model:SM_F971N`
- 지정 셸: `SM-F971N`, Android 17, API 37, `ROAMADB_WIRELESS_SHELL_OK`
- USB와 RoamADB transport 동시 분리 식별: PASS
- USB 분리 뒤 순수 원격 유지: 현장 재시험 대기

#### 필요한 수정·자동 재발방지

Gateway는 로컬 ADB accept 대기 중 휴대폰의 `close` 또는 소켓 종료를 감지해 listener를 즉시 닫도록 수정했다. connect·pairing relay 슬롯을 각각 직렬화해 다음 세션이 이전 listener 정리를 기다리도록 했고, 휴대폰 종료 직후 같은 고정 포트 재게시 시험을 추가했다. 수정 전 14/15에서 신규 시험이 FAIL했고 수정 후 15/15 PASS했다. 수정 GUI를 설치한 뒤 USB 없이 `127.0.0.1:47157 device`와 실제 셸을 다시 확인했다.

Android 17에서 현재 TLS connect 포트를 자동 탐지하거나 변경을 감지해 갱신하는 편의 기능은 후속으로 남는다. 현재 스파이크에서는 무선 디버깅 화면의 일반 포트를 사용자가 다시 저장할 수 있다.

### BUG-2026-020 — Gateway 게시가 먼저 생성한 Android APK를 삭제

- 발견일: 2026-08-25
- 심각도: HIGH
- 상태: CLOSED(전체 빌드·세 자산·체크섬 패키징 PASS)
- 영향: 릴리스 스크립트를 Android→Gateway 순서로 실행할 때 최종 패키징 실패

#### 증상·직접 원인

v0.1.2 전체 빌드에서 Android 테스트·APK·Lint는 성공했지만, 뒤이어 실행한 `publish-gateway.ps1`가 `artifacts/release` 전체를 재귀 삭제했다. 따라서 먼저 생성된 `RoamADB-0.1.2-spike-debug.apk`가 사라지고 `package-release.ps1`가 누락을 탐지해 실패했다.

#### 수정·재발방지

Gateway 게시 스크립트는 전용 중간 출력 `artifacts/gateway/win-x64`만 정리한다. 공유 릴리스 폴더는 경로 안전성을 검사하고 생성만 하며, 자기 Portable 파일만 `-Force`로 교체한다. Android APK·Setup·체크섬 등 다른 빌더의 산출물은 삭제하지 않는다.

#### 재시험 결과

- Android `test assembleDebug lintDebug`: PASS
- Gateway Release build·단위·통합시험: PASS, 15/15
- Installer 생성: PASS
- 최종 패키징: APK·Portable·Setup·`SHA256SUMS.txt` 4개 생성 PASS
- 세 배포 자산의 실제 SHA-256과 `SHA256SUMS.txt`: 일치

### BUG-2026-021 — 잠긴 Pi 계정에서 무암호 sudo를 먼저 제거해 관리자 작업 차단

- 발견일: 2026-09-01
- 심각도: HIGH
- 상태: OPEN(기능 경로 정상, one-shot SD 복구·OS update·재부팅 검증 필요)
- 영향: 현재 `roamadb-bridge`의 패키지 업데이트와 추가 root 설정

#### 증상

Pi 보안 감사에서 중복 `NOPASSWD: ALL` 두 개를 제거한 직후 95개 보류 패키지를 업데이트하려 하자 `sudo`가 비밀번호를 요구했다. 사용자는 포맷 뒤 계정 비밀번호를 정하거나 전달받은 적이 없었다.

#### 직접 원인·확인 증거

권한을 축소하기 전에 계정의 실제 비밀번호 상태와 축소 뒤 유지보수 경로를 확인하지 않았다. 사후 `passwd -S roamadb` 결과는 `L`로, 계정 비밀번호가 잠겨 있어 입력 가능한 sudo 비밀번호 자체가 없었다. 공개키 SSH, USB ADB, USB 테더링과 Tailscale은 계속 정상이지만 일반 root 작업은 차단됐다.

#### 잘못된 작업 순서

1. 서비스·방화벽 설치
2. 중복 전체 무암호 sudo 즉시 제거
3. 그 뒤에야 OS update 시도

올바른 순서는 계정 상태 확인 → update·구성 적용 → 제한 maintenance wrapper 설치·시험 → 전체 sudo 제거 → 새 SSH 세션 회귀시험이어야 한다.

#### 수정·복구 준비

- `install-pi-zero2-bridge.sh`가 OS 전체 업그레이드를 권한 축소보다 먼저 수행하도록 수정했다.
- 전체 sudo 대신 `update|reboot|poweroff`만 받는 root 소유 `roamadb-maintain` wrapper를 추가했다.
- FAT boot 파티션과 systemd-run-generator를 이용하는 one-shot `recover-locked-pi-admin.sh`를 작성했다.
- 복구 payload는 자기 kernel parameter를 제거하고 자동 종료되며 임의 명령 권한을 열지 않는다.
- 구체 절차와 실패 복원을 `라즈베리파이_관리자_복구.md`에 기록했다.

#### 재시험 결과

- 기존 ADB·USB tether·Tailscale SSH: PASS
- 계정 잠김 확인: PASS(문제 재현)
- recovery script shell syntax: PASS
- systemd-run-generator 지원 여부: PASS(현재 Pi binary와 Debian 13 공식 manpage에서 확인)
- 실제 SD one-shot 복구: 미실행
- OS upgrade·정상 재부팅: 미실행
- 최종 sudo 최소 권한: 현장 검증 필요

#### 남은 위험

복구 실기기 시험 전에는 관리자 권한 문제를 CLOSED로 표시하지 않는다. `cmdline.txt`를 잘못 편집하면 부팅 실패가 생길 수 있으므로 원본 백업과 한 줄 형식 보존이 필수다.

## 6. 개별 기록 템플릿

```markdown
### BUG-YYYY-NNN — 제목

- 발견일:
- 심각도:
- 상태:
- 영향 버전·커밋:
- 기기·Android·One UI:
- 네트워크:

#### 증상

#### 재현 방법

1.
2.
3.

#### 예상 결과

#### 실제 결과

#### 직접 원인

#### 확인 증거

- 로그:
- 스크린샷:
- 테스트:
- 관련 코드:

#### 영향 범위

#### 잘못된 기존 접근

#### 수정 내용

#### 자동 재발방지 장치

#### 재시험 결과

- BUILD:
- UNIT:
- INTEGRATION:
- DEVICE E2E:
- REGRESSION:
- REVIEW:

#### 남은 위험
```

## 7. 반복 실패 규칙

- 원인을 확인하지 않은 추측성 수정을 약 5회 이상 반복하지 않는다.
- 같은 실패가 반복되면 증상, 재현, 확인 사실, 시도, 실패 이유와 다음 실험을 먼저 정리한다.
- 브라우저·앱·소스가 다르면 작업경로, 설치 APK 버전, 실행 프로세스와 실제 제공 소스를 확인한다.
- 현장 검증이 필요한 문제는 PASS가 아니라 `현장 검증 필요`로 표시한다.
